// coffeeScript.coffee: Generated by CoffeeScript 2.3.1 (coffeescript.coffee IMMED5 affects ./coffee -v)
(function() {
  // process: ENV={"node":true,"rn":false,"ut":true,"source":"/Users/pete/gitlab/rn/API/Flexbase/A.coffee"}
  var EXPORTED, arraysEqual, trace;

  /*
  A - Array Functions					*** PROJECT AGNOSTIC ***

  WHAT: Node module

  DESCRIPTION

  FEATURES
  -

  NOTES
  -

  TODOs
  -

  KNOWN BUGS:
  -
  */
  //import trace
  trace = require('./trace');

  if (!Array.isArray) {
    Array.isArray = function(arg) {
      return Object.prototype.toString.call(arg) === '[object Array]';
    };
  }

  arraysEqual = function(a, b) {
    var i;
    if (a === b) {
      return true;
    }
    if (a === null || b === null) {
      return false;
    }
    if (a.length !== b.length) {
      return false;
    }
    i = 0;
    while (i < a.length) {
      if (a[i] !== b[i]) {
        return false;
      }
      ++i;
    }
    return true;
  };

  EXPORTED = {
    //if ut
    s_ut: function() {
      var A_UT, UT;
      UT = require('./UT');
      return (new (A_UT = class A_UT extends UT {
        run() {
          return this.t("arraysEqual", function(ut) {
            return this.assert(arraysEqual([1, 2, 3], [1, 2, 3]));
          });
        }

      })).run();
    },
    //endif
    arraysEqual: arraysEqual,
    IS: function(v) {
      return Array.isArray(v);
    },
    who: function() {
      return console.log("A.coffee");
    }
  };

  module.exports = EXPORTED;

}).call(this);
